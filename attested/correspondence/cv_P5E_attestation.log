File "P5E_attestation.cv", line 71, characters 41-42:
Warning: Replication uses the same parameter N with channel cExe as replication at line 71, characters 14-15 with channel cApp. Avoid reusing parameters for multiple replications with different channels to avoid losing precision in the probability bound.
Doing expand get, insert and prove unique annotations... No change.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Run simplify 1 time(s). Fixpoint reached.
No change.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... No change.
Doing merge branches... No change.
Proof of forall m: payload; event(executed(m)) ==> event(approved(m)) failed:
  Found executed(m_3) at 41
  but could not prove event(approved(m))
Trying equivalence uf_cma(sign)... Failed.
Doing remove assignments of binder pkA... Done.
Doing remove assignments of binder skA... Done.
Trying equivalence uf_cma(sign) with r, rk... Transf. OK Proba. computed Transf. done Succeeded.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Run simplify 2 time(s). Fixpoint reached.
Done.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... No change.
Doing merge branches... No change.
Proved query forall m: payload; event(executed(m)) ==> event(approved(m))
===================== Proof starts =======================
Initial state
Game 1 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       return(m_2, (m_1: payload <- m_2; sk: skey <- skA; r <-R sign_seed; sign_r(m_1, sk, r)))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
       if check(m_3, pkA, s) then
       event executed(m_3);
       return()
     ))


Applying expand
  - Expand if/find/let
yields

Game 2 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       m_1: payload <- m_2;
       sk: skey <- skA;
       r <-R sign_seed;
       return(m_2, sign_r(m_1, sk, r))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
       if check(m_3, pkA, s) then
       event executed(m_3);
       return()
     ))


Applying remove assignments of findcond
  - Remove assignments on m_1 (definition removed, all usages removed)
  - Remove assignments on sk (definition removed, all usages removed)
yields

Game 3 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       r <-R sign_seed;
       return(m_2, sign_r(m_2, skA, r))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
       if check(m_3, pkA, s) then
       event executed(m_3);
       return()
     ))


Applying remove assignments of binder pkA
  - Remove assignments on pkA (definition removed, all usages removed)
yields

Game 4 is
     cstart() :=
     rk <-R keyseed;
     skA: skey <- sskgen(rk);
     return(spkgen(rk));
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       r <-R sign_seed;
       return(m_2, sign_r(m_2, skA, r))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
       if check(m_3, spkgen(rk), s) then
       event executed(m_3);
       return()
     ))


Applying remove assignments of binder skA
  - Remove assignments on skA (definition removed, all usages removed)
yields

Game 5 is
     cstart() :=
     rk <-R keyseed;
     return(spkgen(rk));
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       r <-R sign_seed;
       return(m_2, sign_r(m_2, sskgen(rk), r))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
       if check(m_3, spkgen(rk), s) then
       event executed(m_3);
       return()
     ))


Applying equivalence uf_cma(sign) with r, rk [probability Psign(time_1, N)]
  - Equivalence uf_cma(sign) with variables: rk -> r_1, r -> r2
yields

Game 6 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       r <-R sign_seed;
       return(m_2, (x: payload <- m_2; sign_r2(x, skgen2(rk), r)))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
 {32}  if (m1: payload <- m_3; si1: signature <- s; find u = ri <= N suchthat defined(x[ri]) && (m1 = x[ri]) && check2(m1, pkgen2(rk), si1) then true else false) then
       event executed(m_3);
       return()
     ))


Applying expand
  - Expand if/find/let
    - Test at 32 always false
    - Test at 32 always true
yields

Game 7 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       r <-R sign_seed;
       x: payload <- m_2;
       return(m_2, sign_r2(x, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
       m1: payload <- m_3;
       si1: signature <- s;
       find u = ri <= N suchthat defined(x[ri]) && (m1 = x[ri]) && check2(m1, pkgen2(rk), si1) then
       event executed(m_3);
       return()
     ))


Applying remove assignments of findcond
  - Remove assignments on m1 (definition removed, all usages removed)
  - Remove assignments on si1 (definition removed, all usages removed)
  - Remove assignments on x (definition removed, all usages removed)
yields

Game 8 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       r <-R sign_seed;
       return(m_2, sign_r2(m_2, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
 {29}  find u = ri <= N suchthat defined(m_2[ri], r[ri]) && (m_3 = m_2[ri]) && check2(m_3, pkgen2(rk), s) then
       event executed(m_3);
       return()
     ))


Applying simplify
  - Simplification pass
    - Replaced defined condition m_2[ri], r[ri] with m_2[ri] in find at 29
yields

Game 9 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cApp(m_2: payload) :=
       event approved(m_2);
       r <-R sign_seed;
       return(m_2, sign_r2(m_2, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cExe(m_3: payload, s: signature) :=
       find u = ri <= N suchthat defined(m_2[ri]) && (m_3 = m_2[ri]) && check2(m_3, pkgen2(rk), s) then
       event executed(m_3);
       return()
     ))


Proved forall m: payload; event(executed(m)) ==> event(approved(m)) in game 9
Adv[Game 1: forall m: payload; event(executed(m)) ==> event(approved(m))] <= Psign(time_1, N) + Adv[Game 9: forall m: payload; event(executed(m)) ==> event(approved(m))]
Adv[Game 9: forall m: payload; event(executed(m)) ==> event(approved(m))] <= 0
RESULT Proved forall m: payload; event(executed(m)) ==> event(approved(m)) up to probability Psign(time_1, N)
RESULT time_1 = 2 * N * time(([1,N])) + time + (N - 1) * time(check)
All queries proved.
