File "P11link_unforgeable.cv", line 79, characters 42-43:
Warning: Replication uses the same parameter N with channel cVer as replication at line 79, characters 14-15 with channel cAuth. Avoid reusing parameters for multiple replications with different channels to avoid losing precision in the probability bound.
Doing expand get, insert and prove unique annotations... No change.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Run simplify 1 time(s). Fixpoint reached.
No change.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... No change.
Doing merge branches... No change.
Proof of forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u)) failed:
  Found linkAccepted(p_2, u_2) at 55
  but could not prove event(linkIssued(p, u))
Trying equivalence uf_cma(sign)... Failed.
Doing remove assignments of binder pkA... Done.
Doing remove assignments of binder skA... Done.
Trying equivalence uf_cma(sign) with r, rk... Transf. OK Proba. computed Transf. done Succeeded.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Run simplify 2 time(s). Fixpoint reached.
Done.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... No change.
Doing merge branches... No change.
Proved query forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u))
===================== Proof starts =======================
Initial state
Game 1 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       return(p_1, u_1, (m: linkmsg <- mklink(p_1, u_1); sk: skey <- skA; r <-R sign_seed; sign_r(m, sk, r)))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       if check(mklink(p_2, u_2), pkA, s) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying expand
  - Expand if/find/let
yields

Game 2 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       sk: skey <- skA;
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, sk, r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       if check(mklink(p_2, u_2), pkA, s) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of findcond
  - Remove assignments on sk (definition removed, all usages removed)
yields

Game 3 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, skA, r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       if check(mklink(p_2, u_2), pkA, s) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of binder pkA
  - Remove assignments on pkA (definition removed, all usages removed)
yields

Game 4 is
     cstart() :=
     rk <-R keyseed;
     skA: skey <- sskgen(rk);
     return(spkgen(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, skA, r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       if check(mklink(p_2, u_2), spkgen(rk), s) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of binder skA
  - Remove assignments on skA (definition removed, all usages removed)
yields

Game 5 is
     cstart() :=
     rk <-R keyseed;
     return(spkgen(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, sskgen(rk), r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       if check(mklink(p_2, u_2), spkgen(rk), s) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying equivalence uf_cma(sign) with r, rk [probability Psign(time_1, N)]
  - Equivalence uf_cma(sign) with variables: rk -> r_1, r -> r2
yields

Game 6 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, (x: linkmsg <- m; sign_r2(x, skgen2(rk), r)))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
 {43}  if (m1: linkmsg <- mklink(p_2, u_2); si1: signature <- s; find u_3 = ri <= N suchthat defined(x[ri]) && (m1 = x[ri]) && check2(m1, pkgen2(rk), si1) then true else false) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying expand
  - Expand if/find/let
    - Test at 43 always false
    - Test at 43 always true
yields

Game 7 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       x: linkmsg <- m;
       return(p_1, u_1, sign_r2(x, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       m1: linkmsg <- mklink(p_2, u_2);
       si1: signature <- s;
       find u_3 = ri <= N suchthat defined(x[ri]) && (m1 = x[ri]) && check2(m1, pkgen2(rk), si1) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of findcond
  - Remove assignments on si1 (definition removed, all usages removed)
  - Remove assignments on x (definition removed, all usages removed)
yields

Game 8 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r2(m, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       m1: linkmsg <- mklink(p_2, u_2);
 {46}  find u_3 = ri <= N suchthat defined(m[ri], r[ri]) && {49}((m1 = m[ri]) && check2(m1, pkgen2(rk), s)) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying simplify
  - Simplification pass
    - Replaced ((m1 = m[ri]) && check2(m1, pkgen2(rk), s)) with ((p_2 = p_1[ri]) && (u_2 = u_1[ri]) && check2(m1, pkgen2(rk), s)) at 49
    - Replaced defined condition m[ri], r[ri] with u_1[ri], p_1[ri] in find at 46
yields

Game 9 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r2(m, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv, s: signature) :=
       m1: linkmsg <- mklink(p_2, u_2);
       find u_3 = ri <= N suchthat defined(u_1[ri], p_1[ri]) && (p_2 = p_1[ri]) && (u_2 = u_1[ri]) && check2(m1, pkgen2(rk), s) then
       event linkAccepted(p_2, u_2);
       return()
     ))


Proved forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u)) in game 9
Adv[Game 1: forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u))] <= Psign(time_1, N) + Adv[Game 9: forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u))]
Adv[Game 9: forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u))] <= 0
RESULT Proved forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u)) up to probability Psign(time_1, N)
RESULT time_1 = 2 * N * time(mklink) + 2 * N * time(([1,N])) + time + (N - 1) * time(check)
All queries proved.
