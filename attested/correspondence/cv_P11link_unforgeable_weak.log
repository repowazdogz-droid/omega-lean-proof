File "P11link_unforgeable_weak.cv", line 28, characters 42-43:
Warning: Replication uses the same parameter N with channel cVer as replication at line 28, characters 14-15 with channel cAuth. Avoid reusing parameters for multiple replications with different channels to avoid losing precision in the probability bound.
Doing expand get, insert and prove unique annotations... No change.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Run simplify 1 time(s). Fixpoint reached.
No change.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... No change.
Doing merge branches... No change.
Proof of forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u)) failed:
  Found linkAccepted(p_2, u_2) at 45
  but could not prove event(linkIssued(p, u))
Trying equivalence uf_cma(sign)... Failed.
Doing remove assignments of binder pkA... Done.
Doing remove assignments of binder skA... Done.
Trying equivalence uf_cma(sign) with r, rk... Transf. OK Proba. computed Transf. done Succeeded.
Doing simplify (non-expanded game)... No change.
Doing expand... Done.
Doing remove assignments of findcond... Done.
Doing simplify... Run simplify 1 time(s). Fixpoint reached.
No change.
Doing move all binders... No change.
Doing SA rename new without array accesses and remove assignments of findcond... No change.
Doing merge branches... No change.
Proof of forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u)) failed:
  Found linkAccepted(p_2, u_2) at 41
  but could not prove event(linkIssued(p, u))
Trying equivalence uf_cma(sign)... Failed.
===================== Proof starts =======================
Initial state
Game 1 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       return(p_1, u_1, (m: linkmsg <- mklink(p_1, u_1); sk: skey <- skA; r <-R sign_seed; sign_r(m, sk, r)))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying expand
  - Expand if/find/let
yields

Game 2 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       sk: skey <- skA;
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, sk, r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of findcond
  - Remove assignments on sk (definition removed, all usages removed)
yields

Game 3 is
     cstart() :=
     rk <-R keyseed;
     pkA: pkey <- spkgen(rk);
     skA: skey <- sskgen(rk);
     return(pkA);
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, skA, r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of binder pkA
  - Remove assignments on pkA (definition removed, all usages removed)
yields

Game 4 is
     cstart() :=
     rk <-R keyseed;
     skA: skey <- sskgen(rk);
     return(spkgen(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, skA, r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of binder skA
  - Remove assignments on skA (definition removed, all usages removed)
yields

Game 5 is
     cstart() :=
     rk <-R keyseed;
     return(spkgen(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r(m, sskgen(rk), r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying equivalence uf_cma(sign) with r, rk [probability Psign(time_1, N)]
  - Equivalence uf_cma(sign) with variables: rk -> r_1, r -> r2
yields

Game 6 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, (x: linkmsg <- m; sign_r2(x, skgen2(rk), r)))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying expand
  - Expand if/find/let
yields

Game 7 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       x: linkmsg <- m;
       return(p_1, u_1, sign_r2(x, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))


Applying remove assignments of findcond
  - Remove assignments on x (definition removed, all usages removed)
yields

Game 8 is
     cstart() :=
     rk <-R keyseed;
     return(pkgen2(rk));
     ((
       foreach i <= N do
       cAuth(p_1: expv, u_1: expv) :=
       event linkIssued(p_1, u_1);
       m: linkmsg <- mklink(p_1, u_1);
       r <-R sign_seed;
       return(p_1, u_1, sign_r2(m, skgen2(rk), r))
     ) | (
       foreach j <= N do
       cVer(p_2: expv, u_2: expv) :=
       event linkAccepted(p_2, u_2);
       return()
     ))

RESULT time_1 = N * time(mklink) + 2 * N * time(([1,N])) + time + -1 * time(check)
RESULT Could not prove forall u, p: expv; event(linkAccepted(p, u)) ==> event(linkIssued(p, u)).
